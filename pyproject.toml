[tool.poetry]
name = "serverless-lab-render"
version = "1.0.0"
description = "Serverless Lab with Flask and PostgreSQL on Render"
authors = ["Your Name <you@example.com>"]
readme = "README.md"
package-mode = false # <-- ДОБАВЬТЕ ЭТУ СТРОКУ

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
Flask = "3.0.0"
psycopg2 = "2.9.7" # <--- ИЗМЕНЕНО С psycopg2-binary


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
