[tool.poetry]
name = "serverless-lab-render"
version = "1.0.0"
description = "Serverless Lab with Flask and PostgreSQL on Render"
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.12" # Явно указываем Python 3.11
Flask = "3.0.0"
psycopg2-binary = "2.9.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
